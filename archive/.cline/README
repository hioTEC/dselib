# Cline CLI 配置说明

## 📋 配置文件位置

配置文件应该放在项目根目录的 `.cline` 目录下：
```
.cline/
├── config.json          # 你的实际配置文件（不提交到Git）
├── config.json.template # 配置模板（可提交到Git）
└── README.md           # 本说明文件
```

## 🔐 API密钥配置

### 获取Anthropic API密钥

1. 访问 [Anthropic Console](https://console.anthropic.com/)
2. 登录或创建账户
3. 进入 API Keys 页面
4. 创建新的API密钥
5. 复制密钥（注意：只显示一次）

### 配置步骤

1. **复制模板文件**：
   ```bash
   cp .cline/config.json.template .cline/config.json
   ```

2. **编辑配置文件**：
   ```bash
   # 使用你喜欢的编辑器打开
   nano .cline/config.json
   # 或
   code .cline/config.json
   ```

3. **填写API密钥**：
   将 `YOUR_ANTHROPIC_API_KEY_HERE` 替换为你的实际API密钥

## 📝 配置文件模板

创建 `.cline/config.json` 文件，内容如下：

```json
{
  "apiProvider": "anthropic",
  "apiKey": "YOUR_ANTHROPIC_API_KEY_HERE",
  
  "model": "claude-3-5-sonnet-20241022",
  "maxTokens": 8096,
  "temperature": 0.7,
  
  "projectContext": {
    "name": "DSE Library",
    "description": "香港DSE考试试卷资源库",
    "type": "fullstack-web",
    "language": "zh-CN",
    "mainLanguages": ["JavaScript", "Python", "HTML", "CSS"],
    "frameworks": ["Vue 3", "Tailwind CSS", "Python 3"]
  },
  
  "codeStyle": {
    "indent": 2,
    "quotes": "single",
    "semicolons": false
  },
  
  "features": {
    "autoFormat": true,
    "linting": true,
    "autoSave": true,
    "confirmBeforeExecute": true
  }
}
```

## ⚙️ 配置选项说明

### API配置

| 选项 | 说明 | 默认值 | 可选值 |
|-----|------|--------|--------|
| `apiProvider` | API提供商 | `anthropic` | `anthropic`, `openai` |
| `apiKey` | API密钥 | - | 你的密钥 |
| `model` | 使用的模型 | `claude-3-5-sonnet-20241022` | 见下方模型列表 |
| `maxTokens` | 最大token数 | `8096` | 1-200000 |
| `temperature` | 温度参数 | `0.7` | 0.0-1.0 |

### 可用模型

**Anthropic Claude 模型**：
- `claude-3-5-sonnet-20241022` - 最新Sonnet版本（推荐）
- `claude-3-5-sonnet-20240620` - 旧版Sonnet
- `claude-3-opus-20240229` - Opus版本（最强）
- `claude-3-sonnet-20240229` - Sonnet版本
- `claude-3-haiku-20240307` - Haiku版本（最快）

### 项目上下文

```json
"projectContext": {
  "name": "项目名称",
  "description": "项目描述",
  "type": "项目类型",
  "language": "主要语言",
  "mainLanguages": ["语言列表"],
  "frameworks": ["框架列表"]
}
```

### 代码风格

```json
"codeStyle": {
  "indent": 2,          // 缩进空格数
  "quotes": "single",   // 引号类型: single/double
  "semicolons": false,  // 是否使用分号
  "lineWidth": 100      // 行宽限制
}
```

### 功能开关

```json
"features": {
  "autoFormat": true,           // 自动格式化代码
  "linting": true,              // 启用代码检查
  "testing": false,             // 启用测试
  "autoSave": true,             // 自动保存
  "verboseOutput": false,       // 详细输出
  "confirmBeforeExecute": true  // 执行前确认
}
```

## 🔒 安全注意事项

### 重要提醒

⚠️ **绝对不要将包含真实API密钥的配置文件提交到Git仓库！**

### 保护措施

1. **已添加到 `.gitignore`**：
   ```gitignore
   # Cline配置（包含敏感信息）
   .cline/config.json
   .cline/*.local.json
   ```

2. **使用环境变量**（可选）：
   ```bash
   export ANTHROPIC_API_KEY="your-key-here"
   ```

3. **定期轮换密钥**：
   - 定期更新API密钥
   - 删除不再使用的密钥

## 🚀 使用Cline CLI

### 基本命令

```bash
# 启动Cline交互模式
cline

# 指定配置文件
cline --config .cline/config.json

# 指定项目目录
cline --project /path/to/project

# 查看帮助
cline --help

# 查看版本
cline --version
```

### 常用功能

```bash
# 代码审查
cline review

# 生成文档
cline document

# 重构代码
cline refactor

# 运行测试
cline test
```

## 📊 项目特定配置

针对DSE Library项目的推荐配置：

```json
{
  "projectContext": {
    "name": "DSE Library",
    "description": "香港DSE考试试卷资源库 - 提供试卷浏览、搜索和下载功能",
    "type": "fullstack-web",
    "language": "zh-CN",
    
    "mainLanguages": ["JavaScript", "Python", "HTML", "CSS"],
    "frameworks": ["Vue 3", "Tailwind CSS", "Python 3"],
    
    "architecture": {
      "frontend": "Vue 3 + Tailwind CSS + PWA",
      "backend": "Python索引生成器和爬虫",
      "deployment": "Cloudflare Pages",
      "storage": "静态文件 + JSON索引"
    }
  },
  
  "paths": {
    "frontend": "./frontend",
    "admin": "./admin",
    "papers": "./papers",
    "data": "./frontend/public/data",
    "docs": "./docs"
  },
  
  "preferences": {
    "language": "zh-CN",
    "timezone": "Asia/Shanghai",
    "dateFormat": "YYYY-MM-DD"
  }
}
```

## 🔧 故障排除

### 常见问题

**Q: API密钥无效？**
```bash
# 检查密钥格式
# Anthropic密钥格式: sk-ant-...
```

**Q: 配置文件不生效？**
```bash
# 检查JSON语法
cat .cline/config.json | jq .

# 或使用Python检查
python3 -m json.tool .cline/config.json
```

**Q: 权限错误？**
```bash
# 修改文件权限
chmod 600 .cline/config.json
```

**Q: 找不到配置文件？**
```bash
# 检查文件是否存在
ls -la .cline/

# 从模板创建
cp .cline/config.json.template .cline/config.json
```

## 🔄 配置更新

### 更新配置

```bash
# 编辑配置
code .cline/config.json

# 重启Cline使配置生效
cline --reload
```

### 备份配置

```bash
# 备份当前配置
cp .cline/config.json .cline/config.json.backup

# 恢复配置
cp .cline/config.json.backup .cline/config.json
```

## 📚 参考资源

- [Anthropic API文档](https://docs.anthropic.com/)
- [Cline CLI GitHub](https://github.com/anthropics/cline)
- [Claude模型对比](https://docs.anthropic.com/claude/docs/models-overview)

## 💡 最佳实践

1. **使用专用API密钥**：为不同项目创建不同的密钥
2. **设置使用限额**：在Anthropic控制台设置月度限额
3. **监控使用情况**：定期检查API使用统计
4. **本地测试优先**：先在本地测试，避免浪费API调用
5. **版本控制配置模板**：提交模板文件，忽略实际配置

## 📞 获取帮助

如果遇到问题：
1. 查看 [Anthropic文档](https://docs.anthropic.com/)
2. 检查 [Cline Issues](https://github.com/anthropics/cline/issues)
3. 查看项目的 `plans/WSL_MIGRATION_PLAN.md` 文档

---

**最后更新**: 2025-12-18
